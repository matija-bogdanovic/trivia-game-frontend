/**
 * The podium marks. A display constant, not data — it lived in _mock/players
 * next to the fixture standings and stayed behind when those were deleted.
 * Anything below third is its own number.
 */
export const rankBadges: Record<number, string> = { 1: '★', 2: '◆', 3: '▲' };
